model = "gpt-5.3-codex"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Audit infrastructure and deployment readiness.
Prefer render and preflight evidence over assumptions.
Separate repo-ready state from environment-specific blockers such as placeholders, secrets, operator files, and missing tooling.
"""
