## Documentation Expectations

These instructions apply inside `docs/`.

## Working Agreements

- Keep docs aligned with the current code and deployment setup, not aspirational architecture.
- Prefer exact commands, exact paths, and exact constraints.
- Remove stale claims when they no longer match the repo.
- When a code or infra task changes behavior, update all relevant docs instead of only the nearest file.
